<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/custom_shadow.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape
            android:shape="rectangle">
            <gradient
                android:type="radial"
                android:centerX="50%"
                android:centerY="50%"
                android:gradientRadius="60%"
                android:endColor="@android:color/transparent"
                android:centerColor="?attr/colorBackgroundFloating"
                android:startColor="@android:color/transparent"
                android:angle="180" /><!-- Gradient angle -->
            <corners
                android:radius="@dimen/medium_value_size" />
            <padding
                android:left="@dimen/regular_value_size"
                android:right="@dimen/regular_value_size"
                android:top="@dimen/regular_value_size"
                android:bottom="@dimen/medium_value_size" />
        </shape>
    </item>
</layer-list>
